{
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}"
  },
  "dir": "migrations",
  "direction": "up",
  "count": 1,
  "verbose": true,
  "decamelize": true
}
